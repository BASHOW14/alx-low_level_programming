#include "main.h"
/**
 *mul - multiplies two numbers
 *Return: Returns the product of the two numbers
 *@a:first argument
 *@b:second argument
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
