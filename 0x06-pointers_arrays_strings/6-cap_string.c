#include "main.h"
/**
 * cap_string - capitalize the initials
 * Return: Pointer to the modified string
 * @s: string to check
 */
char *cap_string(char *s)
{
	int i;

	i = 0;

	while (s[i] != '\0')
	{
		if (

