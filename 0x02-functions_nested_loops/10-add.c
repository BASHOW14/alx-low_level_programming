#include "main.h"
/**
 * add - add two numbers
 * Return:Returns sum of the two numbers
 * @a: first operand
 * @b:second operand
 */
int add(int a, int b)
{
	int sum;

	sum = 0;
	sum = a + b;
	return (sum);
}
