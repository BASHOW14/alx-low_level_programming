#include "main.h"
/**
 * puts2 - prints every other character
 * @str:stores the address of the character string
 */
void puts2(char *str)
{
	int i;

	i = 0;

	while (s[i] != 0)
	{
		_putchar(s[i]);
		i = i + 2;
	}
}
